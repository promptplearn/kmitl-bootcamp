/* ──────────────────────────────────────────────────────────────
   KMITL Bootcamp — backend config
   To go LIVE across phones: paste your Supabase project URL + anon key below.
   Leave them as-is to run in LOCAL mode (single-device, localStorage) —
   the app still works, it just won't sync between devices.
   Get these from: Supabase dashboard → Project Settings → API.
   ────────────────────────────────────────────────────────────── */
window.KMITL_CONFIG = {
  SUPABASE_URL: "https://xttbivzmumlprlxlybmr.supabase.co",        // e.g. "https://abcd1234.supabase.co"
  SUPABASE_ANON_KEY: "sb_publishable_oMbnNw4amltviiaFrZQB6A_xC6766Vm",   // the long public "anon" key
  ROOM: "kmitl-2026"       // room id — change to run two rooms at once
};
